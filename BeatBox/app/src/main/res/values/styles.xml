<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!--
        colorPrimary属性主要用于工具栏。由于应用名称是显示在工具栏上的， colorPrimary也可以称为应用品牌色。
        colorPrimaryDark用于屏幕顶部的状态栏。注意，只有Lollipop(Android 5.0)以后的系统支持状态栏主题色。对于之前的系统，无论指定什么主题色，状态栏都是不变的黑底色。
        colorAccent主要用于给EditText这样的组件着色，按钮组件不支持着色。这个主题色应该和colorPrimary形成反差效果。
        -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/dark_red</item>
        <item name="colorAccent">@color/gray</item>
        <item name="android:windowBackground">@color/soothing_blue</item>
        <item name="buttonStyle">@style/BeatBoxButton</item>
    </style>

    <style name="BeatBoxButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_beat_box</item>
    </style>

    <!--
    BeatBoxButton.Strong的命名表明，这个新样式继承了BeatBoxButton样式的属性。
    除了通过命名表示样式继承关系，也可以采用指定父样式的方式:
    <style name="StrongBeatBoxButton" parent="@style/BeatBoxButton">
        <item name="android:textStyle">bold</item>
    </style>
    注意：要以主题名的形式指定父主题，有继承关系的两个主题都应处于同一个包中。
    如果是继承自己内部的主题，使用主题名指定父主题即可；如果是继承Android操作系统中的样式或主题，记得使用parent属性。
    -->
    <style name="BeatBoxButton.Strong">
        <item name="android:textStyle">bold</item>
    </style>
</resources>
